
import 'package:flutter/material.dart';

class chooseLangage extends StatelessWidget {
  const chooseLangage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Choose Language"),
      ),
    );
  }
}
